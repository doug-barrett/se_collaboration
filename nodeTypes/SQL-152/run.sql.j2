{{ stage('SQL AUDIT') }}
INSERT INTO {{ ref_no_link(node.location.name, node.name) }} (SYSTEM_START_DATE) SELECT CURRENT_TIMESTAMP

{% if config.runSQL %}
	{{ stage('Run SQL') }}
	{{ config.runSQL }}
{% endif %}
