excludeSelector: "{ name: *LAND_* } OR { name: DS_* } OR { name: STG_ML_*} OR { name: ML_* }"
fileVersion: 1
id: "10"
includeSelector: "{name: STG_*} OR +{ name: DIM_* } OR +{ name: FCT_* } OR {name: AGG_*} OR { location: DATASTORE name: DS_PROD_ZONE_DAY }"
name: Process ALL no Load
type: Job
